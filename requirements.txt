# Fully pinned environment for the current CPU analysis and corrected pipeline.
# Python 3.12 (see .python-version). This is NOT the historical GPU environment:
# legacy run metadata records Python 3.10.12 and the package versions documented
# in docs/reproducibility-environments.md, but omitted several driver/runtime fields.
# The paper itself compiles with `tectonic` and needs none of these Python packages.
#
# Full install:      pip install -r requirements.txt
# Library only:      pip install -e .                 # core CPU analysis
# With GPU training: pip install -e ".[train,dev]"
#
# CPU-only reproduction of every table/figure from the committed scores.parquet
# needs just the "core analysis" block below.

# --- core analysis: metrics, provenance, thresholds, table/figure generation (CPU) ---
numpy==2.5.0
scipy==1.18.0
scikit-learn==1.9.0
pandas==3.0.3
pyarrow==24.0.0
PyYAML==6.0.3
transformers==5.12.1
matplotlib==3.11.0

# --- training + scoring (pipeline steps 4-5; GPU) and HF manifest sourcing (step 1) ---
torch==2.13.0
peft==0.19.1
trl==1.7.0
datasets==5.0.0
accelerate==1.14.0
safetensors==0.8.0

# --- dev / test ---
pytest==9.1.1

# MinHash is deliberately implemented by the pinned NumPy backend in
# guard_research/provenance.py; installing another package must not change hashes.
