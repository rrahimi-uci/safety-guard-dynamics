{
  "schema_version": 3,
  "manifest_id": "paper_c_sta_migration_v1",
  "migrated_on": "2026-07-26",
  "git_revision": "cf57ab2",
  "study_id": "paper_c_specialize_align_mortgage_v1",
  "operation": "copy_not_move",
  "digest_tool": "tools/tree_digest.py",
  "digest_coverage_rule": "all regular files except build residue (__pycache__, *.pyc, .pytest_cache, *.egg-info, .DS_Store) and except the contents of artifacts/, inputs/, build/ -- but .gitkeep placeholders inside those directories ARE included",
  "reverify_command": "python tools/tree_digest.py papers/unified-report/archive/paper_c/specialize_then_align studies/paper-c-specialize-align-mortgage-v1 --tracked-only --exclude provenance/MIGRATION_MANIFEST.json --diff",
  "old_tree": {
    "path": "papers/unified-report/archive/paper_c/specialize_then_align",
    "aggregate_sha256": "1714314984bce4dc26f1437af5aa68f3b0a53dbf404c4bf5bd1fab7853f96e97",
    "file_count": 68,
    "status": "intact_compatibility_surface"
  },
  "new_tree": {
    "path": "studies/paper-c-specialize-align-mortgage-v1",
    "aggregate_sha256": "dfbb45ce1fe91c109ea96c64b049dbf1f8255c98c9b6c0e9049ef439f2e8572b",
    "file_count": 71,
    "status": "phase_4_study_package"
  },
  "pre_migration_snapshot": {
    "aggregate_sha256": "e19a3985cc253379803ff65e85cfadd27fc29b593e3251ac49c50d7fa59fdb66",
    "file_count": 67,
    "taken_before_any_copy": true,
    "note": "Taken with the FIRST digest rule, which excluded every file under artifacts/, inputs/ and build/ including their .gitkeep placeholders, and so is not comparable to the v2 hashes above. Kept verbatim rather than recomputed: it is the record of what was frozen before the copy, and silently restating it under a new rule would destroy the only evidence of the pre-copy state."
  },
  "source_changes_since_snapshot": {
    "files": [
      "PROTOCOL.md",
      "src/paper_c_sta/contracts.py",
      "src/paper_c_sta/ingest.py",
      "src/paper_c_sta/splits.py",
      "tests/test_contracts.py"
    ],
    "reason": "Three defects found by migrating, all fixed in BOTH trees so they stay byte-identical. (1) Fixed-parent repository discovery: parents[2] is correct only at depth 3; at depth 2 it resolved OUTSIDE the repository, reading corpora from the wrong place rather than failing. Replaced with marker-based repository_root(). (2) The storage contract's runs/ location was unreachable: output_path() rejected an absolute <repo>/runs/<slug>/ path and silently resolved a relative 'runs/x' inside the workspace. Added runs_root() and a second permitted root, still fail-closed at exactly two, plus four tests. (3) PROTOCOL.md specified source-group split isolation that is infeasible on the reuse corpus (9 units across 9 datasets vs. four splits at 50/20/15/15). Corrected to the achievable family/content-family closure, held-out-source transfer put out of scope, and validate_split_isolation() documented as reachable only from tests rather than wired into the pipeline."
  },
  "divergence": {
    "only_in_old_tree": [],
    "only_in_new_tree": [
      "artifacts/.gitkeep",
      "environment/README.md",
      "environment/gpu-requirements.txt"
    ],
    "content_differs": [
      "Makefile",
      "README.md"
    ],
    "explanation": "The new package adds an environment record and this manifest, which the predecessor has no equivalent of, rewrites README.md to state that it is a copy that authorizes nothing, and now carries a divergent Makefile because the predecessor archive moved under papers/unified-report/archive/ while the study package remained at studies/. Nothing is missing from the new tree. All source, config, schemas, tests, tools, cloud scripts, manuscript, locks and layout placeholders are otherwise byte-identical.",
    "correction": "An earlier version of this manifest reported one divergent file and a 67/68 file split. That comparison hashed only non-ignored files, so it could not see .gitkeep placeholders living under ignored prefixes -- and the copy had in fact dropped three of them (artifacts/, inputs/, build/). inputs/.gitkeep was tracked source, so the new tree was genuinely incomplete while the check reported it faithful. The placeholders were restored byte-for-byte and the coverage rule is now stated above and printed by the tool on every run.",
    "note": "artifacts/.gitkeep appears as new-tree-only because of the .gitignore asymmetry recorded below: it is trackable under studies/ and excluded outright by papers/unified-report/archive/paper_c/.gitignore, so the predecessor cannot track it. Both trees have the file on disk; only one can commit it. Counts are stated with this manifest excluded."
  },
  "gitignore_asymmetry": {
    "finding": "artifacts/.gitkeep is TRACKABLE in the new tree but IGNORED in the old one; build/.gitkeep is ignored in both.",
    "cause": "The package .gitignore writes `artifacts/**` then `!artifacts/.gitkeep`, but git cannot re-include a file whose parent DIRECTORY is excluded. papers/unified-report/archive/paper_c/.gitignore:7 excludes `artifacts/` outright, which kills the negation there; no such ancestor rule exists under studies/, so it takes effect. The root .gitignore:7 excludes `build/`, which kills that negation in both.",
    "consequence": "None for execution -- writers call mkdir(parents=True, exist_ok=True). It does show up in the tracked-file comparison as a declared divergence, and it means a fresh clone of the predecessor has no artifacts/ directory until something creates it.",
    "not_fixed_here": "Editing papers/unified-report/archive/paper_c/.gitignore or the root .gitignore to make the negations live would change ignore semantics for paths outside this study package, which is beyond a Phase 5 copy. Recorded rather than silently adjusted."
  },
  "authorization": {
    "claim_authorization": false,
    "evidence_state": "development_only",
    "note": "This migration copies a STOPPED study. It authorizes no data build, training, scoring, or claim. No lock was rewritten and no development output gained evidentiary status by being copied."
  },
  "independent_verification": {
    "old_tree": {
      "command": "make -C papers/unified-report/archive/paper_c/specialize_then_align test PY=<abs>",
      "result": "1 failed, 70 passed"
    },
    "new_tree": {
      "command": "make -C studies/paper-c-specialize-align-mortgage-v1 test PY=<abs>",
      "result": "1 failed, 70 passed"
    },
    "declared_failure": "tests/test_candidate_lock.py::test_tracked_candidate_lock_binds_live_sources_and_authorizes_nothing",
    "declared_failure_detail": "ContractError: locked config bytes drifted -- the candidate lock binds live source bytes and the source tree has evolved. Declared expected_fail in studies/registry.yaml for both trees; `make check-locks` errors if it ever starts passing.",
    "identical_outcomes": true,
    "repo_root_resolution": {
      "papers/unified-report/archive/paper_c/specialize_then_align": "safety-guard-dynamics",
      "studies/paper-c-specialize-align-mortgage-v1": "safety-guard-dynamics"
    },
    "note_on_tallies": "Run pytest without adding -q: pyproject sets addopts=\"-q\" already, and a second -q suppresses the count line entirely. The first attempt at this verification read an absent count line rather than a real one.",
    "note_on_test_count": "70 passed, up from 66 at first migration: the four added tests cover the runs/ routing. Both trees must always report the same tally -- that identity is the equivalence evidence, and tests/test_migration_manifest.py in the repository root fails if the trees drift apart."
  },
  "not_migrated": {
    "build/": "vendored Cloud SDK, 530 MB; Phase 5 requires the SDK installed OUTSIDE the repo. The placeholder is present; the contents are not.",
    "artifacts/": "generated development output; inventoried in EXTERNAL_OBJECT_MANIFEST.json (114 objects, 19.15 GiB, development-only)",
    "inputs/": "ignored local corpora, rebuildable from benchmarks/ per the ledger"
  },
  "digest_excludes": [
    "provenance/MIGRATION_MANIFEST.json"
  ],
  "digest_exclude_reason": "This manifest records the hash of the tree it lives in, so it must exclude itself or the recorded value is stale the instant it is written. The exclusion is passed explicitly to the tool rather than hardcoded, so the number below is reproducible.",
  "digest_mode": "tracked_only",
  "digest_mode_reason": "Hashes cover only files git tracks. A working-tree digest is not reproducible in a fresh clone: the first version of this manifest hashed whatever was on disk, so CI -- which has no ignored files -- computed a different hash for an unchanged tree and the enforcement test failed on a clean checkout. Recording a number that only one machine can reproduce is not provenance."
}
